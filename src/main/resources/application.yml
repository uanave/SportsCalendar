server:
  port: 9200

spring:
  datasource:
    url: jdbc:mysql://localhost/calendar?useSSL=false&createDatabaseIfNotExist=true
    username: calendar
    password: calendar
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
  security:
    user:
      name: admin
      password: admin
